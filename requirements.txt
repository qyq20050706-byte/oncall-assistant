# Web 框架
fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.27.0,<0.35.0
pydantic>=2.6.0,<3.0.0
python-multipart>=0.0.9

# HTML 解析
beautifulsoup4>=4.12.0
lxml>=5.0.0

# 检索算法
rank-bm25>=0.2.2

# 语义向量（CPU 版本足够，约 200MB）
sentence-transformers>=2.7.0,<3.0.0

# 兜底检索 + 数值计算
scikit-learn>=1.4.0
numpy>=1.26.0,<2.0.0

# HTTP 客户端（LLM 调用用）
httpx>=0.27.0

# 配置文件支持（如果你用了 .env）
python-dotenv>=1.0.0